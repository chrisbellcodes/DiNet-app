
<div class="container">
  <%  if @current_user %>
  <p> <%= link_to "Host a Party", new_dinner_party_path, class: "btn btn-primary" %></p>
  <% end %>
</div>

<div class="container">
  <% @dinners.each do |dinner| %>
  <div class="card pt-xl-3">
    <!--   <img src="..." class="card-img-top" alt="..."> -->
    <div class="card-body">
        <h5 class="card-title"><%=dinner.name%></h5>
        <%= link_to dinner.name, dinner, class: "btn btn-primary" %>
      </div>
    </div>
  <% end %>
</div>
